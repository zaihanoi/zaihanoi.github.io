# Theme & Markdown Settings
theme: jekyll-theme-primer
markdown: kramdown

kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter: rouge
  auto_ids: true
  math_engine: mathjax

plugins:
  - jekyll-mentions
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-sitemap